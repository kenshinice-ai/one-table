# Luna-Max 产图任务（5 张重出 · 唯一有效版本 · 2026-08-18 更新）

> 使用方式：整份交给 luna-max。**本文件取代此前所有产图 prompt。**
> 进度：600 道全部产完，主线做了一次**全库目检**（600 张按课程分组联络表逐张核对）。结论：**595 张合格**，5 张画错了菜（不是台面色问题，是画面内容与菜名不符），需要重出。另有一对图片互相装反（椰香米布丁 ↔ 甜酱焖猪肉），主线已直接对调，不需要你处理。

---

## Prompt 开始

你是本项目的视觉制作工程师。任务：按清单产图。**你的交付物只有图片文件；不改任何代码、数据、脚本、配置。** 集成（尺寸档、注册、部署）全部由主线接管。

项目根目录：`/Users/llmacbookpro/Library/Mobile Documents/com~apple~CloudDocs/receipt_cal`

### 1. 任务清单（5 张重出）

```bash
npm run recipes:briefs   # 清单里这 5 条带 status: "redo" 与 redoReason
```

| slug                               | 退回原因（现图画成了什么） | 应该画什么                                     |
| ---------------------------------- | -------------------------- | ---------------------------------------------- |
| japanese-mushroom-clear-soup       | 牛油果玉米沙拉             | 日式菌菇清汤：香菇+口蘑+清汤，葱花点缀         |
| indonesian-peanut-sweet-potatoes   | 鸡块                       | 印尼花生酱烤红薯：红薯块淋花生椰浆酱，青柠香菜 |
| indian-lentil-coconut-soup         | 芒果酸奶杯                 | 印度椰香扁豆汤：扁豆番茄汤，孜然姜味，椰浆纹   |
| french-coconut-chocolate-free-pots | 炒蘑菇                     | 法式椰香奶油盏：小杯装凝固椰奶冻，蜂蜜香草     |
| sichuan-pepper-sweet-potato-bites  | 牛肉条                     | 花椒红薯小块：烤红薯块撒孜然辣椒花椒           |

**这五张的共同教训**：画面主体必须是 `ingredients` 字段里的主料。上表五道的主料分别是菌菇、红薯、扁豆、椰浆、红薯——现图里一个都没出现。**产图前先读 brief 的 `ingredients` 第一项，那就是画面主角。**

### 2. 风格规范（沿用 + 两条经验强调）

沿用条款（与已上线 572 张一致）：4:3 · 1600×1200；45° 俯拍为主（汤类可更俯）；单侧柔和窗光；素色哑光陶瓷器皿；构图留 15–25% 呼吸空间；1–2 件极简道具；家常可食的真实状态；份量按 `role`。

**台面色硬规格**（保持现状即可）：中性暖白/米白，对照 `sichuan-cold-noodle-cups.webp` / `french-onion-thyme-soup.webp` / `cantonese-winter-melon-soup.webp`。禁止桃色/陶土色调。主线每批跑色度抽检。

**经验强调一 · 菜名里的形态词就是规格**：菜名中出现的具体形态必须与画面一致——linguine 是长条细扁面（那次退回的教训）、fritters/烙是对应形态、「丁/丝/片/块」按字面。产前对一眼 `titleEn` 和 `titleZh`。

**经验强调二 · "-free" 改良菜是过敏原红线的重点**：`crab-free`、`squid-free`、`pineapple-free` 这类 slug 表示这道菜**特意移除了**该食材——画面绝对不能出现它（你目前处理得很好，继续保持）。

**绝对禁止**（不变）：文字、logo、水印、人物、手、包装、`ingredients` 之外的任何可辨识食材。

### 2b. 边界提醒（这次踩到了）

上一批重出时，注册表 `data/recipes/generated-media.json` 被你那边直接写了。这次图是好的，但这一步**必须留给主线**——它是「照片上线」的开关，写进去就等于跳过质检直接发布。主线已在 `art:adopt` 加了检测：注册表出现非本脚本写入的条目会直接中止并列出 slug。

**你要做的仍然只有一件事：把 `public/media/<slug>.webp` 提交上来。** 不跑 `art:adopt`、不跑 `media:sizes`、不动注册表。

### 3. 交付协议（每批，严格执行）

- **每批 25 张**，完成即提交，不攒。
- **只提交 `public/media/<slug>.webp` 主图**。不跑 `media:sizes`、不动 `data/`、`src/`、`scripts/`。
- 主图 ≤300KB（质量 82–88 内调；落盘前自检）。
- commit message：`art: batch <序号> (<张数> images)`——**张数写实际数**（此前有几批与实数不符，影响对账；从 batch 6 起序号接着排）。
- 遇 `.git/index.lock` 等待重试，不删除锁文件；push 失败时本地保留 commit 稍后重推。**产完记得 push**——未提交的散图主线也会收，但对账更费事。
- 每张自检：食材 ⊆ brief `ingredients`；无文字/人物/手/包装；4:3 · 1600×1200；≤300KB；台面色对照参考图；**形态与 `titleEn`/`titleZh` 一致**。

### 4. 边界（不变）

不改动任何已注册图片；不引入依赖；不动构建配置。节庆场景不改变风格制式——同一张桌子、同一种光，只是菜不同。

## Prompt 结束
