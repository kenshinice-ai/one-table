node_modules
.node_modules.backup-*
.next
.open-next
.wrangler
.generated
package-lock.json
docs/renders
ideas
cloudflare-env.d.ts

# Build output: these payloads ship to every visitor, and pretty-printing them
# inflates the download for no reader benefit.
public/data
src/generated

# Rewritten by `next build` on every run; Next owns its formatting.
next-env.d.ts
